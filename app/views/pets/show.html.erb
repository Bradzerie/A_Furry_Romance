<div class="container-pet">
  <div class="col-12">
    <div class="div">
      <div class="card-pet">
        <h1> <%= @pet.name %> </h1>
          <div id="paragraph">
            <p> Age: <%= @pet.age %></p>
            <p> Species: <%= @pet.species %></p>
            <p> Gender: <%= @pet.gender %></p>
            <p> Price: $<%= @pet.price %></p>
          </div>
        </div>
            <%# <% if @pet.user == current_user %>
            <%= link_to pet_path(@pet),
            data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} do %>
              <i class="fas fa-trash"></i>
            <% end %>
        <%# <% end %>
      </div>
              <%# Below are the buttons  %>
  <button type="button" class="btn my-3 py-2 btn-outline-primary">
    <%= link_to 'Back to Pet Listings', pets_path %>
  </button>
    <button type="button" class="btn py-2 btn-outline-primary">
      <%= link_to 'Make a Booking', new_pet_booking_path(@pet) %>
    </button>
  </div>
</div>

<div class="container-pet">
  <button type="button" class="btn py-2 btn-outline-primary">
  <%= link_to 'All bookings for your pet', pet_bookings_path(@pet) %>
  </button>
</div>
